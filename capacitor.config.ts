import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular-tech-tests',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
